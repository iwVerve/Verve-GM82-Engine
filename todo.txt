looks weird on discord screenshare (looks to be an gm8.2 extension bug)
controller support? (will probably look into only if asked. it's more of a mess then studio)
iwpo compatibility (should only require an update to iwpo, not the engine)
more documentation
